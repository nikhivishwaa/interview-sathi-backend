name: Testing

on:
  push:
    branches:
      - dev
  pull_request:
    branches:
      - dev

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Say Hello
        run: echo "🎉 Hello, World from GitHub Actions on the dev branch!"
