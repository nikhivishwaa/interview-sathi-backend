name: Test Backend

on:
  push:
    branches: [ dev ]

jobs:
  deployment:
    runs-on: ubuntu-latest
    steps:
      - name: Deployement Check
      - run: echo "starting deployment"
    
      - name: Deployement Check
      - run: echo "ending deployment"
